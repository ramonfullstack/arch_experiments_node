import { describe } from 'mocha';

// This should be a integration test
describe('loginController', () => {
  // This should test each possible answer type(Error in serialization, deserialization and one happy path)
});
